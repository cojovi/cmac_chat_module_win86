# Talk to CMAC Environment Variables
# Copy this file to .env for local development

# Note: API keys should be stored in the system keyring via the app settings
# These environment variables are for reference only

# Vite environment variables must be prefixed with VITE_

# OpenWebUI Configuration (Optional - can be set in app settings)
# VITE_OPENWEBUI_BASE_URL=http://localhost:3000
# VITE_OPENWEBUI_MODEL_NAME=llama2

# Logging Level
# RUST_LOG=info

# Development Settings
# NODE_ENV=development
